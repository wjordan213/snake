TODO:
1) implement dirs queue that pops at each move not empty.
  -turn adds to queue when size is less than two
2) change the way the board renders, both view render and board render
3) keep score and display at top right of board
4) increase frame rate and number of squares on the board
5) encorporate sprites into game
6) pause the game.
